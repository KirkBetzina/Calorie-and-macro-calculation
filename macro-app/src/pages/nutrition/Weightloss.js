// let the user select how much weight they would like to lose, and build meal plan suggestion. display MealPlan.js
import React from 'react'

const Weightloss = (props) => {
    return (
        <div>
            <h1>// let the user select how much weight they would like to lose, and build meal plan suggestion. display MealPlan.js</h1>
        </div>
    )
}
export default Weightloss