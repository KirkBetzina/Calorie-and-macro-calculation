import React from 'react'

const Nav = (props) => {
    return (
        <div>
            <h1>Lets try and hamburger menu that has a cool X when clicked, if not, no worries.</h1>
        </div>
    )
}

export default Nav