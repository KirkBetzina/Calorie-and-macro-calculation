// Allow user to turn on or off an alarm that will sound when its time to eat.
import React from "react"
const EatTimer = (props) => {
    return (
        <div>
            <h1>This is the feed time reminde</h1>
        </div>
    )
}
export default EatTimer